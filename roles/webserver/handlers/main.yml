- name: Restart web server
  service:
    name: "{{ webserver_service }}"
    state: restarted

- name: Reload web server configuration
  service:
    name: "{{ webserver_service }}"
    state: reloaded